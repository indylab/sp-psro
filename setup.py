from setuptools import setup

setup(
   name='grl',
   version='1.0',
   description='Multiplayer Game Reinforcement Learning.',
   author='',
   author_email='',
   packages=['grl'],  # same as name
   install_requires=[],  # external packages as dependencies
)
